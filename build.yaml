targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: cybernate_retail_mobile|lib/schema.graphql
          output_dir: 'models'
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: cybernate_retail_mobile|lib/schema.graphql
          output_dir: 'models'
  