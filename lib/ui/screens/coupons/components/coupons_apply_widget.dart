import 'package:flutter/material.dart';

class CouponsApplyWidget extends StatelessWidget {
  const CouponsApplyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
