# import '../fragments/ProductDetailsFragment.graphql'
query ProductById($id: ID!, $channel: String!, $locale: LanguageCodeEnum!) {
  product(id: $id, channel: $channel) {
    ...ProductDetailsFragment
  }
}