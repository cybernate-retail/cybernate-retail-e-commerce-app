# import '../fragments/AvailablePaymentGatewayFromCheckoutTokenFragment.graphql'
query AvailablePaymentGatewayFromCheckoutToken($checkoutToken: UUID!) {
  checkout(token: $checkoutToken) {
    ...AvailablePaymentGatewayFromCheckoutTokenFragment
  }
}
